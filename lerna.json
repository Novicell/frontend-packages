{
  "packages": [
    "packages/core/*",
    "packages/vanilla/*",
    "packages/vue/*",
    "packages/css/*"
  ],
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["master", "main", "feature/*", "feat/*"]
    }
  },
  "changelogPreset": {
    "name": "conventionalcommits",
    "issueUrlFormat": "{{host}}/{{owner}}/{{repository}}/issues/{{id}}"
  }
}
