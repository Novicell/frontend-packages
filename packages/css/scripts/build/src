#!/usr/bin/env bash

# Called from inside packages, will distribute source css
printf "🧱 Building Source CSS: %s 🧱" "${PWD##*/}"
npm run lint:fix && parcel build src/*.css --no-content-hash --no-source-maps --no-minify -d dist/src --no-cache
